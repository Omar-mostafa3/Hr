# =============================================================================
# HR Management System - Backend Configuration
# =============================================================================
# Copy this file to .env and update with your actual values
# Do NOT commit .env to version control - it may contain sensitive data
# =============================================================================

# Database Configuration
# Connection string for your MongoDB instance
MONGODB_URI=mongodb://localhost:27017/hr-main

# JWT Configuration
# Secret key for JWT signing (generate a strong secret for production)
JWT_SECRET=your-super-secret-key-change-this-in-production
# Token expiration time (in hours or time format: '60m', '24h', etc.)
JWT_EXPIRATION=60m

# Server Configuration
# Port on which the backend server will listen
PORT=5000
# Environment (development, staging, production)
NODE_ENV=development

# Frontend Configuration (for CORS)
# URL of the running frontend application
FRONTEND_URL=http://localhost:3000
FRONTEND_URL_ALT=http://localhost:3001

# Email Configuration (Optional - if using email notifications)
# SMTP Host
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
# SMTP User (usually your email address)
SMTP_USER=your-email@gmail.com
# SMTP Password or App-specific password
SMTP_PASS=your-app-password
# From email address
EMAIL_FROM=noreply@hr-system.com

# Logging Configuration
# Log level: error, warn, info, debug
LOG_LEVEL=info

# File Upload Configuration (if using file uploads)
# Maximum file size in bytes (e.g., 10 * 1024 * 1024 for 10MB)
MAX_FILE_SIZE=10485760
# Upload directory
UPLOAD_DIR=./uploads

# =============================================================================
# Production Security Notes:
# - Change JWT_SECRET to a strong random string
# - Use environment-specific MongoDB URIs
# - Enable HTTPS in production
# - Set NODE_ENV=production
# - Use secure SMTP credentials
# - Implement rate limiting
# - Use proper logging and monitoring
# =============================================================================
